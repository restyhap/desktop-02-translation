# Tauri 更新签名密钥生成说明

## 首次使用（已在本会话完成）

签名密钥已在 `~/.tauri/desktop-translation.key` 和 `~/.tauri/desktop-translation.key.pub` 生成。

### 公钥（已填入 tauri.conf.json）
```
untrusted comment: "generated by tauri"
2GoCErAoyLR+JukNb9j2NwuyXhV93M3HYgPsno5Slr4=
```

### 私钥（用于打包签名）
- 路径：`~/.tauri/desktop-translation.key`
- 打包时设置环境变量：`TAURI_SIGNING_PRIVATE_KEY` 和 `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`

## 后续打包发布命令

```bash
# 设置环境变量
export TAURI_SIGNING_PRIVATE_KEY=$(cat ~/.tauri/desktop-translation.key)
export TAURI_SIGNING_PRIVATE_KEY_PASSWORD="你的密码"

# 构建并签名
cd src-tauri
cargo tauri build
# 或
pnpm tauri build
```

## 发布流程
1. 升级 `version`（Cargo.toml + tauri.conf.json + package.json 三处同步）
2. `pnpm tauri build` 生成 `.app.tar.gz` + `.sig` + `latest.json`
3. 将制品部署到服务器
4. 更新服务器上 `latest.json` 的 URL 和签名

## 本地测试
```bash
# 启动本地 HTTP 服务器
python3 -m http.server 8787
# 在 src-tauri 目录下（包含 latest.json 和 .app.tar.gz）
```
